# Connexion MongoDB Atlas — voir CDC section 3.1/3.8
MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net"
MONGODB_DB_NAME="sadic"

# Tenant utilisé en développement tant qu'Auth.js (session -> tenant) n'est
# pas branché (Phase 0, fin) — voir apps/web/lib/currentTenant.ts.
DEV_TENANT_ID="6aa46e58f2ab93f47ce8c75d"
